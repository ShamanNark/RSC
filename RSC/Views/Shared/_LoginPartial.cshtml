@using Microsoft.AspNetCore.Identity
@using RSC.Models

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    @*<form asp-area="" asp-controller="Account" asp-action="Logout" method="post" id="logoutForm" class="navbar-right">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <a asp-area="" asp-controller="Manage" asp-action="Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
                </li>
                <li>
                    <button type="submit" class="btn btn-link navbar-btn navbar-link">Выйти</button>
                </li>
            </ul>
        </form>*@


    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" id="cabinet-link">КАБИНЕТ</a>
        <ul class="dropdown-menu">
            <li><a href="/manage/index/">@UserManager.GetUserName(User)</a></li>
            <li role="separator" class="divider"></li>
            @if (User.IsInRole("ADMIN") || User.IsInRole("OPERATOR"))
            {
                <li><a href="@Url.Action("Index","Admin")">Админ панель</a></li>
            }
            @if ((User.IsInRole("OOBO") || User.IsInRole("ADMIN")) && ViewBag.HasPrdso == "True" )
            {
                <li><a href="@Url.Action("Index","Profile")">Личный кабнет</a></li>
            }
            @if ((User.IsInRole("OOBO") || User.IsInRole("ADMIN")) && (ViewBag.Status == "Approved" && ViewBag.HasPrdso != "True") )
            {
                <li><a href="@Url.Action("Create","PRDSO")">Cоздать ПРДСО</a></li>
            }
            <li> <form asp-area="" asp-controller="Account" asp-action="Logout" method="post">
                    
                     <button type="submit">ВЫЙТИ</button>
                </form>
            </li>
           
        </ul>
    </li>
}
else
{

    <li><a asp-area="" asp-controller="Account" asp-action="Login"  id="cabinet-link">ВОЙТИ</a></li>
}
